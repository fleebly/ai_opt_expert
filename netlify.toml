# Netlify Configuration
# Note: This is a Streamlit application, which runs as a Python backend service.
# Netlify is primarily for static sites, but this config provides a redirect to Railway deployment.

[build]
  publish = "public"
  command = "echo 'Building static redirect page...'"

[build.environment]
  NODE_VERSION = "18"
  PYTHON_VERSION = "3.11"

# Redirect all requests to Railway deployment
[[redirects]]
  from = "/*"
  to = "https://api.ai-opt-expert.com/:splat"
  status = 200
  force = true
  headers = {X-From = "Netlify"}

# Alternative: API-specific redirects (if needed)
# [[redirects]]
#   from = "/api/*"
#   to = "https://api.ai-opt-expert.com/api/:splat"
#   status = 200
#   force = true

